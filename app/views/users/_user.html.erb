
<tr>
  <td><%= link_to "#{user.full_name}", user_path(user.id) %></td>
  <td><%= user.email%></td>
  <td><%= user.sex%></td>
  <td><%= user.birthday%></td>
  <% if can? :edit, user %>
     <td><%= link_to 'edit', edit_user_path(user.id) %></td>
  <%end%>   
</tr>

