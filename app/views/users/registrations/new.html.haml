-content_for :title  do
  ="Sign Up"

-content_for :header do
  =render :partial=>'shared/general_header' 

-content_for :single_panel do
  .signup   
    .title
      Sign Up
    .signup_form_container
      =render :partial=>'users/registrations/new', :locals => { :user => @user }
    .third_party
      .text
        Sign up with your existing account with
      =render :partial=>'users/sessions/third_party_signin'   
